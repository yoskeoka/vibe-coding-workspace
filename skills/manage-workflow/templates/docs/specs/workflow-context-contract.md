# Spec: Workflow Context Contract

`AGENTS.md` is the short universal guard and task router. `AI_WORKFLOW.md` is
the linkable lifecycle reference; workflow skills own phase procedures and link
back to it instead of repeating lifecycle rules. Read relevant specs and ADRs
on demand. PR authors fill only applicable template sections, with `review-task`
identifying conditional closure and evidence details.
