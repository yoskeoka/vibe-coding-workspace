Place execution plans here.
Format: `<name>.md`, where `<name>` is descriptive kebab-case without a numeric prefix.
For execution branches, the filename stem must match the branch description: `feat/initial-setup` uses `initial-setup.md`.
When a plan is completed and the PR is ready, move the file to `../done/`.
