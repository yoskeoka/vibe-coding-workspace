# Restore the Codex hooks spec index target

## Summary

`docs/specs/README.md` lists `codex-hooks.md`, but that file is absent from the
workspace. Restore the document or replace the stale index entry after
confirming the intended owner and scope of the child-hook dispatcher contract.

## Why this is separate

The Stop-goal reminder has its own focused black-box contract. Reconstructing
or retiring an older dispatcher document is unrelated to its safe scope and
would broaden the implementation PR.

## Status

Open.
